#include <bits/stdc++.h>
using namespace std;

struct Node{
    int data;
    Node* next;
};

int main()
{
    Node* head;
    Node* first, second, third;

    first->data = 10;
    first->next = &second;
    return 0;
}